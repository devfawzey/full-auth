
Todo:
  ☐ one line install
    ☐ npm i http-status-code jsonwebtoken dotenv cors bcryptjs express-async-errors express-rate-limit helmet joi mongoose rate limiter xss-clean


  ☐ user can upload images with their id

  ☐ when register user asked for (name,email,password) => then register , then asked for verified otp
  ☐ otp is send in user creation
  user {
    name:
    otp:
    password:
    isverified:
  }